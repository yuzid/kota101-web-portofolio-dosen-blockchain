# docker-compose.yml - MONOLITHIC DEVELOPMENT ENVIRONMENT
# Single application container (server + client integrated)
# Untuk production dengan Amazon RDS, gunakan docker-compose.prod.yml

services:
  # Database Off-Chain Relasional
  postgres_db:
    image: postgres:15-alpine
    container_name: jtk_portfolio_db
    environment:
      POSTGRES_USER: portfolio_user
      POSTGRES_PASSWORD: secretpassword
      POSTGRES_DB: portfolio_blockchain_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  # Jaringan Blockchain Node (MultiChain Placeholder / Simulation Service)
  multichain_node:
    image: ubuntu:latest
    container_name: jtk_blockchain_node
    command: tail -f /dev/null
    ports:
      - "4242:4242"

  # Monolithic Application (Server + Client)
  app:
    build: .
    container_name: jtk_portfolio_app
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=development
      - DB_HOST=postgres_db
      - DB_USER=portfolio_user
      - DB_PASSWORD=secretpassword
      - DB_NAME=portfolio_blockchain_db
      - MULTICHAIN_RPC_URL=http://multichain_node:4242
      - AWS_S3_BUCKET=dosen-portfolio-bucket
    depends_on:
      - postgres_db
      - multichain_node

volumes:
  pgdata: