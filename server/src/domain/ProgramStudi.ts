// server/src/domain/ProgramStudi.ts
export class ProgramStudi {
  private kode: string;
  private nama: string;

  constructor(kode: string, nama: string) {
    this.kode = kode;
    this.nama = nama;
  }
}
